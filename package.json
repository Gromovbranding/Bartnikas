{
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^14.18.0 || >=16.0.0"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev -o",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "nuxt-purgecss": "^2.0.0",
    "sass": "^1.56.1"
  },
  "dependencies": {}
}
